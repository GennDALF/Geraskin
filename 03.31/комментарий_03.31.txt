  ====================  легенда  ====================

Верно      — всё хорошо и комментариев не требует
Замечания  — в целом верно, но можно лучше / комментарии
Ошибки     — что-то или всё пошло не так
Неверно    — условие задачи неверно интерпретировано
Нет ответа — файл с решением задачи отсутствует



  =====================  задачи  ====================

117. Замечания:
        а) лишнее действие по перезаписыванию списком локальной переменной s, можно сразу return s.split()

118. Замечания:
        а) лишнее действие по перезаписыванию переменной s, можно сразу input().lower()
        потому что метод lower() применяется к возвращаемому значению функции, а функция input() всегда возвращает объект str

120. Ошибки:
        а) задание на строковые методы, а потому неиспользование метода join() считаю ошибкой
     Замечания:
        а) l = input().split() здесь было бы уместнее, чем цикл while
        б) переменная k, объявленная до функции, существует в глобальном пространстве имён
        переменная k в функции существует в локальном пространстве имён функции
        это разные переменные и они сслыаются на разные области памяти

122. Замечания:
        а) m = input().lower().split()
        б) имена переменных стоит делать более смыслосодержащими; так, на вход вы принимаете слова, почему бы тогда не использовать имя words вместо ни о чём не говорящего имени m
        в) в основной программе вы ни разу не используете индекс кроме как для получения одного элемента – это значит, что можно использовать перебор элементов списка без индексов:
        
            words = []
            for word in words:
                pass
        
        г) а вот в функции уместнее было бы использовать for i in range(len(word)) – цикл for лучше оптимизирован для выполнения таких задач и выполняется быстрее, чем написанный аналогичным образом while

123. Нет ответа




  =====================  баллы  =====================

117. 3/3
118. 3/4
120. 3/5
122. 3/4
123. 0/3